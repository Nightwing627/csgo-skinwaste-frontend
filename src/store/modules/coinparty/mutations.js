export const setIsActive = (state, val) => {
  state.active = val
}

export const setIsJoined = (state, val) => {
  state.joined = val
}

export const setCoinparty = (state, val) => {
  state.coinparty = val
}
